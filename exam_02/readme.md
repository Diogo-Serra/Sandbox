# 42 Exam Practice – exam_02 (C Programming)

**Exam Rules**  
- Only the files you create/submit will be evaluated.  
- Allowed functions for the whole exam (unless the specific exercise forbids some):  
  `write`, `malloc`, `free`  
- No other external functions are allowed (no `printf`, `str*` from libc, etc.) unless explicitly authorized in the exercise.  

---

### Level 1

Turn in files: name of the chosen function + `_main.c` if needed.

| Function/Program       | Description                                                                                 |
|------------------------|---------------------------------------------------------------------------------------------|
| `first_word`           | Display the first word of a string (followed by '\n')                                       |
| `fizzbuzz`             | Display numbers from 1 to 100 (fizz / buzz / fizzbuzz rules)                                |
| `ft_strcpy`            | Reproduce the behavior of `strcpy`                                                          |
| `ft_strlen`            | Reproduce the behavior of `strlen`                                                          |
| `ft_swap`              | Swap the contents of two integers                                                           |
| `putstr`               | Display a string on the standard output                                                     |
| `repeat_alpha`         | Repeat each alphabetical character as many times as its position in the alphabet           |
| `rev_print`            | Display a string in reverse (followed by '\n')                                              |
| `rot_13`               | Apply ROT13 encryption to a string                                                          |
| `rotone`               | Rotate every letter of the string by one (a → b, z → a, etc.)                               |
| `search_and_replace`   | Replace every occurrence of a char by another in a string                                   |
| `ulstr`                | Convert a string to uppercase/lowercase alternating (uLStR style)                          |

---

### Level 2

| Function/Program       | Description                                                                                 |
|------------------------|---------------------------------------------------------------------------------------------|
| `alpha_mirror`         | Replace each letter with the 13th letter after it in the alphabet (mirror)                 |
| `camel_to_snake`       | Convert camelCase string to snake_case                                                      |
| `do_op`                | Simple calculator with two integers and one operator (+ - * / %)                           |
| `ft_atoi`              | Reproduce the behavior of `atoi`                                                            |
| `ft_strcmp`            | Reproduce the behavior of `strcmp`                                                          |
| `ft_strcspn`           | Reproduce the behavior of `strcspn`                                                         |
| `ft_strdup`            | Reproduce the behavior of `strdup`                                                          |
| `ft_strpbrk`           | Reproduce the behavior of `strpbrk`                                                         |
| `ft_strrev`            | Reverse a string in place                                                                   |
| `ft_strspn`            | Reproduce the behavior of `strspn`                                                          |
| `inter`                | Display the common characters of two strings in the order they appear in the first one    |
| `is_power_of_2`        | Return 1 if the number is a power of 2, 0 otherwise                                         |
| `last_word`            | Display the last word of a string                                                          |
| `max`                  | Return the biggest number in an array                                                       |
| `print_bits`           | Display the binary representation of an unsigned char                                      |
| `reverse_bits`         | Reverse the order of bits of an unsigned char                                               |
| `snake_to_camel`       | Convert snake_case string to camelCase                                                      |
| `swap_bits`            | Swap the 4 low bits with the 4 high bits of an unsigned char                               |
| `union`                | Display the union of two strings (unique characters in order of appearance)                |
| `wdmatch`              | Display the first string if it only contains characters present in the second one         |

---

### Level 3

| Function/Program       | Description                                                                                 |
|------------------------|---------------------------------------------------------------------------------------------|
| `add_prime_sum`        | Display the sum of all prime numbers up to the given parameter                             |
| `epur_str`             | Clean a string (remove unnecessary spaces and tabs)                                         |
| `expand_str`           | Insert 3 spaces between each word of a string                                               |
| `ft_atoi_base`         | Convert a string representing a number in a given base to int                               |
| `ft_list_size`         | Return the size of a linked list                                                            |
| `ft_range`             | Return an array of ints containing all values between start and end (inclusive)            |
| `ft_rrange`            | Same as ft_range but in reverse order                                                       |
| `hidenp`               | Check if the first string is completely hidden inside the second one (needle in haystack) |
| `lcm`                  | Return the least common multiple of two numbers                                            |
| `paramsum`             | Display the number of arguments passed to the program                                       |
| `pgcd`                 | Display the greatest common divisor of two numbers                                          |
| `print_hex`            | Display a number in hexadecimal (lowercase, with "0x" prefix)                               |
| `rstr_capitalizer`     | Capitalize the last letter of each word and lowercase the rest                              |
| `str_capitalizer`      | Capitalize the first letter of each word and lowercase the rest                             |
| `tab_mult`             | Display the multiplication table (1 to 9)                                                   |

---

### Level 4

| Function/Program          | Description                                                                                 |
|---------------------------|---------------------------------------------------------------------------------------------|
| `flood_fill`              | Implement the flood fill algorithm on a 2D char table                                      |
| `fprime`                  | Display the prime factors of a given number in ascending order                             |
| `ft_itoa`                 | Convert an integer to an allocated string                                                   |
| `ft_list_foreach`         | Apply a function to each element of a linked list                                           |
| `ft_list_remove_if`       | Remove from the list every element that matches a reference data according to a compare function |
| `ft_split`                | Split a string using a charset of delimiters, return an array of strings                    |
| `rev_wstr`                | Display a string in reverse word order (but each word stays in normal order)                |
| `rostring`                | Rotate the first word of the string to the end (like a rotation)                            |
| `sort_int_tab`            | Sort an array of integers in ascending order                                                |
| `sort_list`               | Sort a linked list using a comparison function (ascending order)                           |

---

**Good luck**  
