

def dfs_generator(grid: list[list[str]]) -> list[list[str]]:
    visited: set[tuple[int, int]] = set()
    stack: set[tuple[int, int]] = {(0, 0)}

    while stack is not None:
        
    print(visited)
    print(stack)
